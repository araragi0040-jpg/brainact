# v021 外部エンジン

## Brian2
`pip install -r requirements-brian2.txt`

## NEST Simulator
`pip install -r requirements-nest.txt`

## The Virtual Brain
`pip install -r requirements-tvb.txt`

Vercel標準環境ではNativeとRegional Mass Liteを使用します。Brian2・NEST・TVBはローカルまたは専用計算サーバーへ導入してください。Regional Mass LiteはTVB本体ではなく、領域レベルの操作フローを確認する内蔵近似モデルです。
