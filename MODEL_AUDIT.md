# v018既存モデル点検と修正

v019～v021の開発前に、v018の計算・API・表示構造を点検しました。

## 修正した問題

1. **異なる出力尺度の混在**
   - Native／Brian2／NESTは発火イベントを出力します。
   - Regional Mass／TVBは連続的な領域活動を画面用の活動相当値へ変換します。
   - v018では両者が同じ「発火数」として比較される箇所がありました。
   - v019以降は出力単位を保持し、異なる単位間では一致度を算出しません。

2. **NEST importの重複**
   - `api/index.py`に同一importが2回ありました。
   - 1回へ整理しました。

3. **テスト実行時のパス依存**
   - `python backend/test_api.py`では実行場所によって`api`を見つけられない場合がありました。
   - テスト側でプロジェクトルートを`sys.path`へ追加し、ルートから直接実行できます。

4. **領域モデルの左右半球配分**
   - 活動値を常に半分ずつ左右へ分けていました。
   - 実際に生成された左右ニューロン数の比率で配分するよう修正しました。

5. **TVB接続行列の符号処理**
   - 抑制性符号が構造接続の集約時に正負相殺を起こし、接続自体が消える可能性がありました。
   - 構造的な結合量として絶対値を集約し、局所的な興奮／抑制とは分けて扱います。

## 残る制限

- 19領域の結合強度は、実測connectomeではなく概念設定を含みます。
- v021同梱メッシュは軽量な参照形状で、MRIから抽出した被験者固有形状ではありません。
- マルチスケール結合則は初期仮説モデルで、生物学的妥当性を保証しません。
- Brian2／NEST／TVB本体は、各パッケージを導入した専用環境で最終検証が必要です。
